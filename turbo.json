{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["DATABASE_URL", "EVE_*", "SUPERMEMORY_*", "TELEGRAM_*", "BLOB_*", "VERCEL_*"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["DATABASE_URL", "EVE_*", "SUPERMEMORY_*", "TELEGRAM_*", "BLOB_*", "VERCEL_*", "COMPOSIO_*", "AI_GATEWAY_*"]
    }
  }
}
